# 🏆 Azalea Ultimate Cloud Scoreboard

نظام عداد نتائج سحابي متكامل (SaaS-grade)، مصمم خصيصاً للبث المباشر (OBS) والتحكم عبر Stream Deck. يتميز بحفظ البيانات سحابياً، دعم تعدد الأطوار (Multi-Mode)، وتحكم كامل في التصميم.

## ✨ المميزات الحصرية (V2.0)

* **💾 حفظ تلقائي ودائم (Database Persistence):**
    * يعتمد على `JSONBlob` كقاعدة بيانات خارجية.
    * لا تضيع النتائج أبداً عند إعادة تشغيل السيرفر أو انقطاع النت.
* **🔄 نظام تعدد الأطوار (Multi-Mode Engine):**
    * دعم كامل لـ 4 أطوار: **1v1, 2v2, 3v3, 4v4**.
    * كل طور يحتفظ بنتائجه وأرقامه القياسية بشكل منفصل.
    * **زر التبديل الذكي (Cycle Mode):** زر واحد للتنقل بين الأطوار بالتتابع.
* **🎨 لوحة تحكم إبداعية (Advanced Admin Panel):**
    * تحكم في الألوان، النصوص، الأحجام.
    * **تحكم هندسي:** سماكة الإطار، انحناء الزوايا (Radius)، وشفافية الظلال.
    * **إخفاء/إظهار:** خيار لإخفاء علامة الطور (Mode Badge) من الشاشة.
* **🔥 أنميشن تفاعلي:**
    * تأثيرات بصرية عند كسر الأرقام القياسية (ذهبي للفوز، تشويش للخسارة).
    * النصوص تخرج عن حدود الإطار (Pop-out effect).

---

## 📂 ملفات المشروع

تأكد من وجود الملفات الأربعة الأساسية لضمان عمل النظام:

1.  **`server.js`** (Backend):
    * يحتوي على منطق السيرفر، الاتصال بقاعدة البيانات، وإدارة الأطوار.
2.  **`package.json`** (Config):
    * يحتوي على تعريف المكتبات (`axios`, `socket.io`, `express`).
3.  **`admin.html`** (Control Panel):
    * واجهة التحكم بالمتصفح.
4.  **`ScoreBoard.html`** (Frontend):
    * ملف العرض الذي يتم وضعه داخل OBS.

---

## 🚀 دليل التثبيت السريع

### 1. إعداد السيرفر (Render + GitHub)
* ارفع ملفات السيرفر (`server.js`, `package.json`, `admin.html`).
* تأكد من وضع رابط `JSONBlob` الخاص بك داخل `server.js`.

### 2. إعداد OBS
* أضف **Browser Source**.
* فعل **Local File** واختر `ScoreBoard.html`.
* الأبعاد: `Width: 800` / `Height: 600`.

### 3. إعداد Stream Deck (التحكم)
استخدم إضافة **API Ninja** أو **HTTP Request**.

#### 🎮 أزرار التحكم (Standard):
هذه الأزرار "ذكية"، فهي تتحكم في الطور النشط حالياً ولا تحتاج لتغيير.

| الوظيفة | الرابط (API URL) | الملاحظات |
| :--- | :--- | :--- |
| **زيادة الفوز** | `/api/set?action=win_inc` | يزيد نقطة للطور الحالي |
| **إنقاص الفوز** | `/api/set?action=win_dec` | ينقص نقطة |
| **زيادة الخسارة** | `/api/set?action=loss_inc` | -- |
| **إنقاص الخسارة** | `/api/set?action=loss_dec` | -- |
| **تصفير النتيجة** | `/api/set?action=reset` | يصفر (0-0) للطور الحالي فقط |
| **تصفير الرانك** | `/api/set?action=reset_records` | يصفر "أعلى رقم" للطور الحالي |

#### 🔄 زر تبديل الطور (Cycle Mode):
زر واحد يقوم بالتبديل: `1v1` -> `2v2` -> `3v3` -> `4v4` -> `1v1`...

| الوظيفة | الرابط (API URL) | Data Extraction |
| :--- | :--- | :--- |
| **الطور التالي** | `/api/set?action=next_mode` | `mode` |

---

## 🎛️ دليل لوحة التحكم (Admin Panel)

للدخول للوحة التحكم: `https://[RABIT-SERVERK].onrender.com/admin`

### الأقسام:
1.  **Stats Grid:** يعرض لك النتائج الحية والريكورد للطور الحالي.
2.  **Mode Manager:**
    * جدول يعرض كل الأطوار.
    * أزرار لتفعيل أي طور، أو تصفير نتائجه عن بعد.
3.  **Design & Style:**
    * **Show Mode Badge:** (Checkbox) لإظهار أو إخفاء أيقونة "MODE: 1v1" من شاشة OBS.
    * **Styling:** تحكم في الـ Radius لجعل العداد دائرياً أو مربعاً، والـ Shadow للظلال.

---

## ⚠️ حل المشاكل الشائعة

**1. السيرفر تعطل (Exited with status 1):**
* غالباً بسبب نقص في `package.json`. تأكد من وجود مكتبة `"axios": "^1.6.0"`.
* قم بعمل **Clear Build Cache** من إعدادات Render.

**2. النتائج لا تُحفظ بعد الريستارت:**
* تأكد أن رابط `JSONBlob` في `server.js` هو رابط الـ API وليس رابط المتصفح.
* الصيغة الصحيحة: `https://jsonblob.com/api/jsonBlob/YOUR_ID`.

**3. علامة الطور (Badge) لا تظهر:**
* تأكد من تفعيل خيار **Show Mode Badge** في صفحة الأدمن والضغط على **Save**.

---

**Developed by:** Azalea Systems 
**Version:** Ultimate-Cloud-V2.5
