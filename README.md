# 🏆 Azalea Cloud Scoreboard System

نظام عداد نتائج سحابي متطور (Real-time Cloud Scoreboard) مخصص للبث المباشر (OBS)، يتيح التحكم في النتائج وتخصيص التصميم لحظياً من أي مكان في العالم، مع مؤثرات بصرية تفاعلية.

## ✨ المميزات الرئيسية

* **☁️ تزامن سحابي لحظي:** يعتمد على `Socket.io` و `Node.js` لتحديث النتائج في أجزاء من الثانية.
* **🎮 تحكم متعدد الأطراف:** إمكانية التحكم في العداد من (Stream Deck) الخاص بك، ومن صديقك (عن بعد) في نفس الوقت.
* **🎨 لوحة تحكم شاملة (Admin Panel):** صفحة ويب لتعديل الألوان، النصوص، الخطوط، والأحجام دون الحاجة لكتابة كود.
* **🔥 أنميشن ذكي (Record Breaker):**
    * تأثيرات عادية عند تغيير الأرقام.
    * **تأثير ذهبي (Gold):** عند كسر الرقم القياسي للفوز.
    * **تأثير تشويش (Glitch):** عند كسر الرقم القياسي للخسارة.
* **📐 تصميم CSS متقدم:** استخدام تقنية `Clipping` للسماح للنصوص بالخروج عن إطار العداد (Pop-out) بينما تبقى الخلفية واللمعان داخل الإطار.

---

## 📂 هيكلية المشروع

المشروع يتكون من جزأين رئيسيين:

1.  **السيرفر (Backend):**
    * مستضاف على منصة سحابية (مثل Render).
    * الملفات: `server.js`, `package.json`, `admin.html`.
    * وظيفته: حفظ الأرقام، إدارة الاتصال، وتوجيه الأوامر.

2.  **العميل (Frontend - OBS):**
    * ملف محلي على جهاز البث.
    * الملف: `ScoreBoard.html`.
    * وظيفته: عرض التصميم وتشغيل الأنميشن بناءً على أوامر السيرفر.

---

## 🚀 طريقة التثبيت والتشغيل

### 1. إعداد السيرفر (Server Side)
* يتم رفع ملفات السيرفر على استضافة تدعم Node.js.
* يتم تثبيت المكتبات المطلوبة: `express`, `socket.io`, `cors`.

### 2. إعداد OBS (Client Side)
1.  أضف مصدر **Browser Source** في OBS.
2.  قم بتفعيل خيار **Local File**.
3.  اختر ملف `ScoreBoard.html`.
4.  الأبعاد المقترحة: `800px` (عرض) × `600px` (ارتفاع).

### 3. إعداد Stream Deck (Remote Control)
استخدم إضافة **API Ninja** أو **HTTP Request** لإرسال الأوامر.
* **نوع الطلب:** `GET`
* **استخراج البيانات (Data Path):** `win` أو `loss`

---

## 🔗 دليل الروابط (API Endpoints)

استبدل `[YOUR_SERVER_URL]` برابط السيرفر الخاص بك.

| الوظيفة | الرابط (Endpoint) | الوصف |
| :--- | :--- | :--- |
| **زيادة الفوز** | `/api/set?action=win_inc` | يزيد الفوز +1 |
| **إنقاص الفوز** | `/api/set?action=win_dec` | ينقص الفوز -1 |
| **زيادة الخسارة** | `/api/set?action=loss_inc` | يزيد الخسارة +1 |
| **إنقاص الخسارة** | `/api/set?action=loss_dec` | ينقص الخسارة -1 |
| **تصفير النتائج** | `/api/set?action=reset` | يعيد النتائج الحالية إلى 0-0 |
| **تصفير الرانك** | `/api/set?action=reset_records` | يصفر "أعلى رقم قياسي" محفوظ |
| **قراءة البيانات** | `/api/get` | لعرض الأرقام على أزرار Stream Deck |

---

## 🛠️ التخصيص (Customization)

يمكنك تغيير مظهر العداد بالكامل من خلال لوحة التحكم:
`[YOUR_SERVER_URL]/admin`

**الخيارات المتاحة:**
* تغيير نصوص العناوين (مثلاً: "فريق أ" بدل "WIN").
* تغيير الألوان (النصوص، الإطارات، الخلفيات).
* التحكم في الأبعاد (العرض، الارتفاع، المسافات).
* **Styling:** التحكم في انحناء الحواف (Radius)، سماكة الإطار، والظلال.
* **Layout:** التبديل بين الوضع الأفقي، العمودي، أو العربي (RTL).

---

## ⚠️ ملاحظات هامة

1.  **حالة السكون (Sleep Mode):** إذا كنت تستخدم استضافة مجانية، قد يتأخر السيرفر في الاستجابة لأول ضغطة زر (30-50 ثانية) حتى يستيقظ. يفضل ضغط زر قبل البث بدقيقة.
2.  **الأمان:** رابط الأدمن (`/admin`) متاح لمن يملك الرابط، لذا حافظ على سرية رابط السيرفر الخاص بك.

---

**تم التطوير بواسطة:** Azalea 
**الإصدار:** Final-Cloud-V1
